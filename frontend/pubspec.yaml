name: opamp_lab_frontend
description: "Virtual lab for Op-Amp Differentiator and Integrator experiments."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ">=1.1.0 <2.0.0"
  provider: ">=6.0.0 <7.0.0"
  shared_preferences: ">=2.2.0 <3.0.0"
  fl_chart: ">=0.66.0 <1.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ">=3.0.0 <5.0.0"

flutter:
  uses-material-design: true
